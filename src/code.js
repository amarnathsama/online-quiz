import React from "react";
import Header from "./header";
var Code = React.createClass({
    render() {
        return (
            <div>
                <Header />
                This is an online quiz system.
            </div>
        );
    },
});

export default Code;
